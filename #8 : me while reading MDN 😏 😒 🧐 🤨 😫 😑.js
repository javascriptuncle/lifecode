// me while reading MDN 😏 😒 🧐 🤨 😫 😑
'use strict'; // read full artile

mylife.read(start("😏",
	MDNLINK.link(											// 😏 => 😒 some IMP link
		subLinks(subLinks(subLinks(			// 😒 => 🧐  might be IMP
			subLinks(subLinks(subLinks(		// 🧐 => 🙄 also might be IMP
				subLinks(subLinks())				// 🤨 => 😫 where am I !!!
				.endOfThePage("😓").followupLinks("🤯 😨") // "Realized I am in loop! 😑"
			)))))))
	),
	end(("IMP 🥺") => { // never ends 🥺

		// like, share and follow
		if('❤👌 my work') {
			console.log('🙏 Stay ❤️ tuned 🙏');
		}
	})
)