// me in office 😎 😅 🤓

mylife.office.work.onNewWork.subscribe((newTask)=>{
	newTask.start().code().test().deploy().reTest().update().end();
}).meanWhile(newTask != 'me'){
	switch (mood) {
		case 'breakTime 😋' || 'videos 👨‍💻' || 'news & stories 😯' || 'gossips 🗣️ 😁' || '🥳':
			break;

		default:
			'timepass';		// 😏 😪 😑
	}
};

// like, share and follow
if ('✨ 👌 my work') {
	console.log('🙏 Stay ❤️ tuned 🙏');
}