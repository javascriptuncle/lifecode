// my talk with testers 🙂 😅 🙃

// after entering inputs
if(you.click("button")){
	you.will.get("Expected Output")
}

// ** beneath(js) stuff **
// onButtonclick.call.someDeadlyFuntion()"
someDeadlyFuntion = () => {
	var usefulData = this.getUniversalInformation()
	var validatedData = this.parseAndValidate(usefulData);
	var simpleResult = this.dosomeDeadlyStuff(validatedData);
	return simpleResult;
}

// like, share and follow
('❤👌 my work') ? 'Please 👍 😄 🤝 ' : "🙏 Stay ❤️ tuned 🙏"