// me when confused 🤨 😅 😂 🤣

// disable BTN using (&&AND ||OR !NOT) Logic
myLife.onWork().confusedState((task)=>{

	// options
	(value1 && value2) OR (value1 OR value2) OR (!value1 && !value2) OR (!value1 OR !value2) OR (value1 && !value2) OR (!value1 && value2) OR (value1 OR !value2) OR (!value1 OR value2) OR  !(value1 && value2) OR (true)
	
	// I ask my self
	// 🤨 😅 😂 🤣 "which will work!";
	
	// Using which one you get confused?
	console.log('🙏 Let me know in Comments 🙏');

	// like, share and follow
	if ('❤👌 my work') {
		console.log('🙏 Stay ❤️ tuned 🙏');
	}
})